import { create } from 'twrnc';

// Create a tailwind instance with the configuration
const tw = create(require('../../tailwind.config.js'));

// Export the tw function for use in components
export default tw;
